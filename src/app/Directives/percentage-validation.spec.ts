import { PercentageValidation } from './percentage-validation';

describe('PercentageValidation', () => {
  it('should create an instance', () => {
    const directive = new PercentageValidation();
    expect(directive).toBeTruthy();
  });
});
