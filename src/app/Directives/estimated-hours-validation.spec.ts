import { EstimatedHoursValidation } from './estimated-hours-validation';

describe('EstimatedHoursValidation', () => {
  it('should create an instance', () => {
    const directive = new EstimatedHoursValidation();
    expect(directive).toBeTruthy();
  });
});
